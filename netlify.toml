[build]
  command = "STATIC=1 npm run build"
  publish = "dist/client"

# SPA fallback so client-side routes resolve on hard refresh / deep links.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
